package es.sd.Controllers;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestParam;

@Controller
public class ConsultsController {

	/*
	 * @Autowired private AnuncioRepository repAnuncios;
	 * 
	 * @RequestMapping(value = "/mostrar") public String mostrar(Model
	 * model, @RequestParam(value = "name") String nombre,
	 * 
	 * @RequestParam(value = "asunt") String asunto) {
	 * 
	 * model.addAttribute("anuncios", repAnuncios.findByNombreAndAsunto(nombre,
	 * asunto));
	 * 
	 * return "mostrarAnuncio"; }
	 */
}
